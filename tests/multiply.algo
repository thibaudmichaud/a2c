algorithme fonction multiply : entier
  parametres locaux
    entier     a, b
  variables
    entier     p, i
debut
  p <- 0
  pour i <- 1 jusqu'a b faire
    p <- p + a
  fin pour
  retourne p
fin algorithme fonction multiply

debut
  ecrire(multiply(3, 4))
fin
