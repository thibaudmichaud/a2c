algorithme procedure test
variables
  entier a, b
  reel c
debut
  a <- 0
  c <- 4.5
  si (a = b) alors
    b <- a + b
  sinon
    a <- a - b
  fin si
fin algorithme procedure test

variables
  booleen variable_globale
debut
  test()
fin
