[X] Utiliser les bons numéros de ligne/colonne dans les erreurs
--  Fonctions prédéfinies:
  [X] ecrire
  [ ] lire
  [ ] allouer
  [ ] liberer
  ... ?
[ ] Trouver une façon plus propre de gérer les erreurs de syntaxe
[ ] ajouter les headers manquant quand certaine fonctions prédéfinies sont
utilisées (ecrire -> stdio.h, allouer -> stdlib.h, etc.)
   idée d'implem: créer une liste globale dans typechecking.c, et la remplir au
fur et à mesure que l'AST est parcouru, puis la passer en parametre à
print_prog()
[ ] Code de retour du programme (!= 0 quand il y a une erreur)
[ ] bibliothèque standard
